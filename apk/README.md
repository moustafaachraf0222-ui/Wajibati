# Wajibati Android APK

`Wajibati-debug.apk` is a debug-signed Android build for testing and direct installation.

Build it again with:

```powershell
npm run apk:debug
```

For public store distribution, create a release build signed with a private release keystore instead of using the debug APK.
